paths:
    migrations: ../db/migrations
    seeds: ../db/seeds

environments:
    default_migration_table: phinxlog
    default_database: development

    development:
        adapter: mysql
        host: db
        name: slim_mysql_data
        user: root
        pass: password
        port: 3306
        charset: utf8
